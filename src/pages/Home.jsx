import React from 'react';

import ContentList from '../components/ContentList';

const Home = () => {
    return (
        <div>
            <ContentList/>
        </div>
    );
};

export default Home;